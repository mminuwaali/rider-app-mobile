import { View } from "react-native";

interface ITransactionCardProps {}

export function TransactionCard(props: ITransactionCardProps) {
  return <View className="p-4 rounded-md bg-gray-100"></View>;
}
